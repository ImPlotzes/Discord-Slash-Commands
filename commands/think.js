// Command stucture for when I want to change it.
// Changing it is done manually so this 'structure' isn't 
// actually being used anywhere in the code.
const structure = {
    name: "think",
    description: "Plotzes Commands is thinking...",
};


export async function handleThink(request, requestBody) {
    // Literally do nothing so it'll always keep thinking...
}
